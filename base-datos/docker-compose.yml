version: '3.8'

services:
  mysql_db:
    image: mysql:9.6.0
    container_name: repuestos_mysql_container
    restart: always
    env_file:
      - .env
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
